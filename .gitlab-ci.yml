image: node:8.11.3

staging:
  stage: deploy
  script:
    - yarn
    - yarn build
    - git add . --verbose
    - git config --global user.email "nderitukelvin19@gmail.com"
    - git config --global user.name "CI"
    - git commit -m 'Dev Build...' --verbose
    - git push origin dev --verbose
  only:
  - dev
